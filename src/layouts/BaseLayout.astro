---
import '../styles/global.css';
import { ViewTransitions } from 'astro:transitions';

export interface Props {
  title?: string;
  description?: string;
  ogImage?: string;
}

const { 
  title = 'South Florida Industrial Real Estate | Ariel Realty Group',
  description = 'Expert tenant representation for premium warehouse and industrial properties across Miami-Dade, Broward, and Palm Beach counties. Fortune 500 level service.',
  ogImage = '/og-image.jpg'
} = Astro.props;

const canonicalURL = new URL(Astro.url.pathname, Astro.site);
const currentPath = Astro.url.pathname;
---

<!DOCTYPE html>
<html lang="en" class="scroll-smooth">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <meta name="description" content={description} />
  <link rel="canonical" href={canonicalURL} />
  
  <!-- Preconnect to font sources for performance -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  
  <!-- Critical CSS for above-the-fold content -->
  <style>
    /* Critical loading styles */
    .loading { opacity: 0; }
    .loaded { opacity: 1; transition: opacity 0.3s ease; }
  </style>
  
  <!-- Favicon with multiple formats -->
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
  <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
  
  <!-- Open Graph / Facebook -->
  <meta property="og:type" content="website" />
  <meta property="og:url" content={canonicalURL} />
  <meta property="og:title" content={title} />
  <meta property="og:description" content={description} />
  <meta property="og:image" content={new URL(ogImage, Astro.url)} />
  <meta property="og:locale" content="en_US" />
  <meta property="og:site_name" content="Ariel Realty Group" />
  
  <!-- Twitter -->
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:url" content={canonicalURL} />
  <meta name="twitter:title" content={title} />
  <meta name="twitter:description" content={description} />
  <meta name="twitter:image" content={new URL(ogImage, Astro.url)} />
  <meta name="twitter:creator" content="@arielrealty" />
  
  <!-- Additional SEO -->
  <meta name="robots" content="index, follow" />
  <meta name="googlebot" content="index, follow" />
  <meta name="theme-color" content="#0f172a" />
  
  <!-- Schema.org markup for enhanced SEO -->
  <script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@type": "RealEstateAgent",
    "name": "Ariel Realty Group",
    "description": "South Florida industrial real estate tenant representation",
    "url": "https://laec.netlify.app",
    "telephone": "(954) 937-9667",
    "email": "info@arielrealty.com",
    "address": {
      "@type": "PostalAddress",
      "addressRegion": "FL",
      "addressCountry": "US"
    },
    "areaServed": [
      {
        "@type": "Place",
        "name": "Miami-Dade County, FL"
      },
      {
        "@type": "Place", 
        "name": "Broward County, FL"
      },
      {
        "@type": "Place",
        "name": "Palm Beach County, FL"
      }
    ],
    "serviceType": "Industrial Real Estate Tenant Representation",
    "hasOfferCatalog": {
      "@type": "OfferCatalog",
      "name": "Industrial Properties",
      "itemListElement": [
        {
          "@type": "Offer",
          "itemOffered": {
            "@type": "Product",
            "name": "Warehouse Space"
          }
        },
        {
          "@type": "Offer", 
          "itemOffered": {
            "@type": "Product",
            "name": "Industrial Space"
          }
        }
      ]
    },
    "sameAs": [
      "https://linkedin.com/company/ariel-realty-group"
    ]
  }
  </script>
  
  <ViewTransitions />
</head>

<body class="loading" id="top">
  <!-- Skip to main content for accessibility -->
  <a href="#main-content" class="sr-only focus:not-sr-only focus:absolute focus:top-4 focus:left-4 z-50 btn-primary">
    Skip to main content
  </a>

  <!-- Premium Navigation Header -->
  <header class="sticky top-0 z-50 transition-all duration-300" id="main-header">
    <nav class="bg-white border-b shadow-sm" style="background-color: rgba(255, 255, 255, 0.95); backdrop-filter: blur(12px); border-bottom-color: rgba(226, 232, 240, 0.5);" aria-label="Main navigation">
      <div class="container mx-auto px-4 max-w-7xl">
        <div class="flex justify-between items-center h-20">
          <!-- Logo -->
          <div class="flex items-center">
            <a href="/" class="flex items-center space-x-3 group" aria-label="Ariel Realty Group - Home">
              <!-- Modern logo design -->
              <div class="relative">
                <div class="w-10 h-10 bg-gradient-to-br from-slate-900 to-blue-600 rounded-xl flex items-center justify-center group-hover:shadow-lg transition-all duration-300">
                  <span class="text-white font-bold text-lg">A</span>
                </div>
                <div class="absolute -inset-1 bg-gradient-to-br from-slate-900 to-blue-600 rounded-xl blur opacity-0 group-hover:opacity-20 transition-opacity duration-300"></div>
              </div>
              <div class="hidden sm:block">
                <div class="text-heading-md font-bold text-slate-900 leading-none">
                  Ariel Realty Group
                </div>
                <div class="text-body-sm text-slate-600">
                  Industrial Real Estate
                </div>
              </div>
            </a>
          </div>
          
          <!-- Desktop Navigation -->
          <div class="hidden lg:flex items-center space-x-8">
            <a href="/" 
               class={`nav-link ${currentPath === '/' ? 'nav-link-active' : ''}`}
               aria-current={currentPath === '/' ? 'page' : undefined}>
              Home
            </a>
            <a href="/warehouses" 
               class={`nav-link ${currentPath.startsWith('/warehouses') ? 'nav-link-active' : ''}`}
               aria-current={currentPath.startsWith('/warehouses') ? 'page' : undefined}>
              Properties
            </a>
            <a href="/about" 
               class={`nav-link ${currentPath === '/about' ? 'nav-link-active' : ''}`}
               aria-current={currentPath === '/about' ? 'page' : undefined}>
              About Us
            </a>
            <a href="/services" 
               class={`nav-link ${currentPath === '/services' ? 'nav-link-active' : ''}`}
               aria-current={currentPath === '/services' ? 'page' : undefined}>
              Services
            </a>
            <a href="/contact" 
               class={`nav-link ${currentPath === '/contact' ? 'nav-link-active' : ''}`}
               aria-current={currentPath === '/contact' ? 'page' : undefined}>
              Contact
            </a>
          </div>
          
          <!-- Call to Action -->
          <div class="hidden md:flex items-center space-x-4">
            <a href="tel:+19549379667" 
               class="text-body-md font-medium text-slate-700 hover:text-blue-600 transition-colors flex items-center">
              <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
              (954) 937-9667
            </a>
            <a href="/#consultation" class="btn-primary">
              Free Consultation
            </a>
          </div>
          
          <!-- Mobile Menu Button -->
          <button 
            type="button" 
            class="lg:hidden p-2 rounded-lg text-slate-700 hover:bg-slate-100 focus:outline-none focus:ring-2 focus:ring-slate-900" 
            id="mobile-menu-button"
            aria-expanded="false"
            aria-controls="mobile-menu"
            aria-label="Toggle navigation menu"
          >
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" id="menu-icon">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
            </svg>
            <svg class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24" id="close-icon">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"/>
            </svg>
          </button>
        </div>
        
        <!-- Mobile Navigation Menu -->
        <div class="lg:hidden hidden" id="mobile-menu">
          <div class="px-2 pt-2 pb-6 space-y-1 bg-white border-t border-slate-200">
            <a href="/" 
               class={`mobile-nav-link ${currentPath === '/' ? 'mobile-nav-link-active' : ''}`}>
              Home
            </a>
            <a href="/warehouses" 
               class={`mobile-nav-link ${currentPath.startsWith('/warehouses') ? 'mobile-nav-link-active' : ''}`}>
              Properties
            </a>
            <a href="/about" 
               class={`mobile-nav-link ${currentPath === '/about' ? 'mobile-nav-link-active' : ''}`}>
              About Us
            </a>
            <a href="/services" 
               class={`mobile-nav-link ${currentPath === '/services' ? 'mobile-nav-link-active' : ''}`}>
              Services
            </a>
            <a href="/contact" 
               class={`mobile-nav-link ${currentPath === '/contact' ? 'mobile-nav-link-active' : ''}`}>
              Contact
            </a>
            
            <!-- Mobile CTAs -->
            <div class="pt-4 border-t border-slate-200 space-y-3">
              <a href="tel:+19549379667" class="mobile-nav-link flex items-center">
                <svg class="w-5 h-5 mr-3" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                </svg>
                Call (954) 937-9667
              </a>
              <a href="/#consultation" class="block btn-primary text-center mx-2">
                Free Consultation
              </a>
            </div>
          </div>
        </div>
      </div>
    </nav>
  </header>

  <!-- Main Content -->
  <main id="main-content" role="main">
    <slot />
  </main>

  <!-- Premium Footer -->
  <footer class="bg-slate-900 text-white">
    <!-- Main Footer Content -->
    <div class="container mx-auto px-4 max-w-7xl py-16">
      <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-8">
        <!-- Company Info -->
        <div class="lg:col-span-2">
          <div class="flex items-center space-x-3 mb-6">
            <div class="w-12 h-12 bg-gradient-to-br from-white to-blue-200 rounded-xl flex items-center justify-center">
              <span class="text-slate-900 font-bold text-xl">A</span>
            </div>
            <div>
              <div class="text-heading-md font-bold leading-none">
                Ariel Realty Group
              </div>
              <div class="text-body-sm text-slate-400">
                Industrial Real Estate Excellence
              </div>
            </div>
          </div>
          
          <p class="text-body-md text-slate-300 mb-6 max-w-md">
            Premier tenant representation for industrial real estate across South Florida. 
            Expert guidance, zero cost to tenants, maximum value delivered.
          </p>
          
          <!-- Contact Info -->
          <div class="space-y-3">
            <a href="tel:+19549379667" class="flex items-center text-slate-300 hover:text-white transition-colors group">
              <div class="w-10 h-10 bg-slate-800 rounded-lg flex items-center justify-center mr-3 group-hover:bg-slate-700 transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                </svg>
              </div>
              <div>
                <div class="font-medium">(954) 937-9667</div>
                <div class="text-body-sm text-slate-400">Call for immediate assistance</div>
              </div>
            </a>
            
            <a href="mailto:info@arielrealty.com" class="flex items-center text-slate-300 hover:text-white transition-colors group">
              <div class="w-10 h-10 bg-slate-800 rounded-lg flex items-center justify-center mr-3 group-hover:bg-slate-700 transition-colors">
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 7.89a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                </svg>
              </div>
              <div>
                <div class="font-medium">info@arielrealty.com</div>
                <div class="text-body-sm text-slate-400">Email us your requirements</div>
              </div>
            </a>
          </div>
        </div>
        
        <!-- Quick Links -->
        <div>
          <h3 class="text-heading-md font-semibold mb-6">Quick Links</h3>
          <ul class="space-y-3">
            <li>
              <a href="/warehouses" class="text-slate-300 hover:text-white transition-colors">
                Available Properties
              </a>
            </li>
            <li>
              <a href="/services" class="text-slate-300 hover:text-white transition-colors">
                Our Services
              </a>
            </li>
            <li>
              <a href="/about" class="text-slate-300 hover:text-white transition-colors">
                About Us
              </a>
            </li>
            <li>
              <a href="/contact" class="text-slate-300 hover:text-white transition-colors">
                Contact
              </a>
            </li>
            <li>
              <a href="/#consultation" class="text-slate-300 hover:text-white transition-colors">
                Free Consultation
              </a>
            </li>
          </ul>
        </div>
        
        <!-- Service Areas -->
        <div>
          <h3 class="text-heading-md font-semibold mb-6">Service Areas</h3>
          <ul class="space-y-3">
            <li class="text-slate-300">Miami-Dade County</li>
            <li class="text-slate-300">Broward County</li>
            <li class="text-slate-300">Palm Beach County</li>
            <li class="text-slate-300">Port Everglades</li>
            <li class="text-slate-300">Miami International Airport</li>
          </ul>
          
          <!-- Social Links -->
          <div class="mt-8">
            <h4 class="text-body-lg font-semibold mb-4">Follow Us</h4>
            <div class="flex space-x-4">
              <a href="#" class="w-10 h-10 bg-slate-800 rounded-lg flex items-center justify-center hover:bg-slate-700 transition-colors" aria-label="LinkedIn">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
                </svg>
              </a>
              <a href="#" class="w-10 h-10 bg-slate-800 rounded-lg flex items-center justify-center hover:bg-slate-700 transition-colors" aria-label="Facebook">
                <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
                  <path d="M24 12.073c0-6.627-5.373-12-12-12s-12 5.373-12 12c0 5.99 4.388 10.954 10.125 11.854v-8.385H7.078v-3.47h3.047V9.43c0-3.007 1.792-4.669 4.533-4.669 1.312 0 2.686.235 2.686.235v2.953H15.83c-1.491 0-1.956.925-1.956 1.874v2.25h3.328l-.532 3.47h-2.796v8.385C19.612 23.027 24 18.062 24 12.073z"/>
                </svg>
              </a>
            </div>
          </div>
        </div>
      </div>
    </div>
    
    <!-- Footer Bottom -->
    <div class="border-t border-slate-800">
      <div class="container mx-auto px-4 max-w-7xl py-6">
        <div class="flex flex-col md:flex-row justify-between items-center">
          <div class="text-body-sm text-slate-400 mb-4 md:mb-0">
            © 2025 Ariel Realty Group. All rights reserved.
          </div>
          <div class="flex space-x-6 text-body-sm">
            <a href="/privacy" class="text-slate-400 hover:text-white transition-colors">
              Privacy Policy
            </a>
            <a href="/terms" class="text-slate-400 hover:text-white transition-colors">
              Terms of Service
            </a>
            <a href="/accessibility" class="text-slate-400 hover:text-white transition-colors">
              Accessibility
            </a>
          </div>
        </div>
      </div>
    </div>
  </footer>

  <!-- Back to Top Button -->
  <button 
    id="back-to-top" 
    class="fixed bottom-8 right-8 w-12 h-12 bg-slate-900 text-white rounded-full shadow-lg hover:bg-slate-800 focus:outline-none focus:ring-2 focus:ring-slate-900 focus:ring-offset-2 opacity-0 invisible transition-all duration-300 z-40"
    aria-label="Back to top"
  >
    <svg class="w-6 h-6 mx-auto" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 10l7-7m0 0l7 7m-7-7v18"/>
    </svg>
  </button>

  <style>
    /* Navigation Styles */
    .nav-link {
      @apply text-body-md font-medium text-slate-700 hover:text-slate-900 transition-colors relative py-2;
    }
    
    .nav-link-active {
      @apply text-slate-900;
    }
    
    .nav-link-active::after {
      content: '';
      @apply absolute bottom-0 left-0 w-full h-0.5 bg-slate-900;
    }
    
    .mobile-nav-link {
      @apply block px-3 py-3 text-body-md font-medium text-slate-700 hover:text-slate-900 hover:bg-slate-50 rounded-lg transition-colors;
    }
    
    .mobile-nav-link-active {
      @apply text-slate-900 bg-slate-100;
    }
    
    /* Header scroll effect */
    .header-scrolled {
      @apply bg-white shadow-md;
      background-color: rgba(255, 255, 255, 0.98);
      backdrop-filter: blur(12px);
    }
    
    /* Smooth loading animation */
    .loading { 
      opacity: 0; 
    }
    
    .loaded { 
      opacity: 1; 
      transition: opacity 0.3s ease; 
    }
  </style>

  <script>
    document.addEventListener('DOMContentLoaded', function() {
      const body = document.body;
      const header = document.getElementById('main-header');
      const mobileMenuButton = document.getElementById('mobile-menu-button');
      const mobileMenu = document.getElementById('mobile-menu');
      const menuIcon = document.getElementById('menu-icon');
      const closeIcon = document.getElementById('close-icon');
      const backToTopButton = document.getElementById('back-to-top');
      
      // Page loaded
      body.classList.remove('loading');
      body.classList.add('loaded');
      
      // Mobile menu toggle
      mobileMenuButton?.addEventListener('click', function() {
        const isExpanded = this.getAttribute('aria-expanded') === 'true';
        
        if (isExpanded) {
          // Close menu
          this.setAttribute('aria-expanded', 'false');
          mobileMenu?.classList.add('hidden');
          menuIcon?.classList.remove('hidden');
          closeIcon?.classList.add('hidden');
        } else {
          // Open menu
          this.setAttribute('aria-expanded', 'true');
          mobileMenu?.classList.remove('hidden');
          menuIcon?.classList.add('hidden');
          closeIcon?.classList.remove('hidden');
        }
      });
      
      // Close mobile menu when clicking outside
      document.addEventListener('click', function(event) {
        const target = event.target as Node;
        const isClickInsideMenu = mobileMenuButton?.contains(target) || mobileMenu?.contains(target);
        
        if (!isClickInsideMenu && !mobileMenu?.classList.contains('hidden')) {
          mobileMenuButton?.setAttribute('aria-expanded', 'false');
          mobileMenu?.classList.add('hidden');
          menuIcon?.classList.remove('hidden');
          closeIcon?.classList.add('hidden');
        }
      });
      
      // Header scroll effect
      let lastScrollY = window.scrollY;
      
      window.addEventListener('scroll', function() {
        const currentScrollY = window.scrollY;
        
        if (currentScrollY > 100) {
          header?.classList.add('header-scrolled');
        } else {
          header?.classList.remove('header-scrolled');
        }
        
        // Back to top button
        if (currentScrollY > 300) {
          backToTopButton?.classList.remove('opacity-0', 'invisible');
          backToTopButton?.classList.add('opacity-100', 'visible');
        } else {
          backToTopButton?.classList.add('opacity-0', 'invisible');
          backToTopButton?.classList.remove('opacity-100', 'visible');
        }
        
        lastScrollY = currentScrollY;
      }, { passive: true });
      
      // Back to top functionality
      backToTopButton?.addEventListener('click', function() {
        window.scrollTo({
          top: 0,
          behavior: 'smooth'
        });
      });
      
      // Smooth scroll for anchor links
      document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
          e.preventDefault();
          const targetId = (this as HTMLAnchorElement).getAttribute('href');
          if (targetId === '#' || !targetId) return;
          
          const target = document.querySelector(targetId);
          if (target) {
            const headerHeight = header?.offsetHeight || 80;
            const targetPosition = target.getBoundingClientRect().top + window.pageYOffset - headerHeight;
            
            window.scrollTo({
              top: targetPosition,
              behavior: 'smooth'
            });
            
            // Close mobile menu if open
            mobileMenuButton?.setAttribute('aria-expanded', 'false');
            mobileMenu?.classList.add('hidden');
            menuIcon?.classList.remove('hidden');
            closeIcon?.classList.add('hidden');
          }
        });
      });
      
      // Keyboard navigation for accessibility
      document.addEventListener('keydown', function(e) {
        if (e.key === 'Escape' && !mobileMenu?.classList.contains('hidden')) {
          mobileMenuButton?.setAttribute('aria-expanded', 'false');
          mobileMenu?.classList.add('hidden');
          menuIcon?.classList.remove('hidden');
          closeIcon?.classList.add('hidden');
          mobileMenuButton?.focus();
        }
      });
    });
  </script>
</body>
</html>