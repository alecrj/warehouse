---
// Updated contact page with split hero layout: form + glassmorphism contact cards
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';

// Define types for warehouse options
interface WarehouseOption {
  title: string;
  slug: string;
}

// Get warehouses for the form dropdown
let allWarehouses: WarehouseOption[] = [];
try {
  const warehouseCollection = await getCollection('warehouses');
  allWarehouses = warehouseCollection
    .filter(w => w.data.availability === 'available')
    .map(w => ({ title: w.data.title, slug: w.data.slug }));
} catch (error) {
  console.log('Could not load warehouses for dropdown');
}
---

<BaseLayout 
  title="Contact Warehouse Locating | Get Matched With Warehouses Today"
  description="Tell us what you need. We'll send qualified options and set up tours. Free tenant representation across South Florida."
>
  <Navigation />
  
  <main>
    <!-- Hero Section - Optimized Above the Fold -->
    <section class="relative bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 overflow-hidden">
      <!-- Background Pattern -->
      <div class="absolute inset-0">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl animate-float"></div>
        <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl animate-float-reverse"></div>
        <div class="absolute inset-0 bg-pattern-dots opacity-10"></div>
      </div>
      
      <div class="relative z-10 w-full max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-16 pb-8">
        <div class="grid lg:grid-cols-2 gap-6 items-center min-h-[55vh]">
          
          <!-- Left Column: Headline + Contact Cards -->
          <div class="animate-on-scroll">
            <h1 class="text-4xl sm:text-5xl lg:text-6xl font-black text-white leading-[0.85] tracking-tight mb-4 mt-8">
              GET MATCHED WITH
              <span class="block bg-gradient-to-r from-primary-400 to-blue-400 bg-clip-text text-transparent">
                WAREHOUSES TODAY
              </span>
            </h1>
            
            <p class="text-lg lg:text-xl text-gray-300 leading-relaxed mb-4 max-w-xl">
              Tell us what you need. We'll send qualified options and set up tours.
            </p>

            <p class="text-base text-gray-400 leading-relaxed mb-6 max-w-3xl">
              <span class="text-white font-semibold">Completely free service.</span> Our fee is paid by landlords.
            </p>

            <!-- Contact Cards - Glassmorphism Style -->
            <div class="space-y-4">
              <!-- Phone Card -->
              <a 
                href="tel:954-937-9667" 
                class="group block p-5 bg-white/10 backdrop-blur-xl rounded-3xl border border-white/20 shadow-2xl hover:shadow-3xl transition-all duration-500 hover:scale-105 hover:bg-white/15 relative overflow-hidden before:absolute before:inset-0 before:bg-gradient-to-br before:from-white/20 before:via-transparent before:to-transparent before:opacity-0 hover:before:opacity-100 before:transition-opacity before:duration-500"
              >
                <div class="relative z-10 flex items-center space-x-4">
                  <div class="w-12 h-12 bg-green-500/20 backdrop-blur-sm rounded-2xl flex items-center justify-center flex-shrink-0 group-hover:bg-green-500/30 transition-colors duration-300">
                    <svg class="w-6 h-6 text-green-400 group-hover:text-green-300 transition-colors duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                    </svg>
                  </div>
                  <div>
                    <div class="text-sm font-semibold text-white/80 mb-1 group-hover:text-white transition-colors duration-300">Call Us Direct</div>
                    <div class="text-xl font-black text-white mb-1 group-hover:text-green-200 transition-colors duration-300">
                      (954) 937-9667
                    </div>
                    <div class="text-white/60 text-sm group-hover:text-white/80 transition-colors duration-300">Available 7 days a week</div>
                  </div>
                </div>
              </a>

              <!-- Email Card -->
              <a 
                href="mailto:info@warehouselocating.com" 
                class="group block p-5 bg-white/10 backdrop-blur-xl rounded-3xl border border-white/20 shadow-2xl hover:shadow-3xl transition-all duration-500 hover:scale-105 hover:bg-white/15 relative overflow-hidden before:absolute before:inset-0 before:bg-gradient-to-br before:from-white/20 before:via-transparent before:to-transparent before:opacity-0 hover:before:opacity-100 before:transition-opacity before:duration-500"
              >
                <div class="relative z-10 flex items-center space-x-4">
                  <div class="w-12 h-12 bg-blue-500/20 backdrop-blur-sm rounded-2xl flex items-center justify-center flex-shrink-0 group-hover:bg-blue-500/30 transition-colors duration-300">
                    <svg class="w-6 h-6 text-blue-400 group-hover:text-blue-300 transition-colors duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 3.26a2 2 0 001.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                    </svg>
                  </div>
                  <div>
                    <div class="text-sm font-semibold text-white/80 mb-1 group-hover:text-white transition-colors duration-300">Email Anytime</div>
                    <div class="text-xl font-black text-white mb-1 group-hover:text-blue-200 transition-colors duration-300">
                      info@warehouselocating.com
                    </div>
                    <div class="text-white/60 text-sm group-hover:text-white/80 transition-colors duration-300">We respond within 24 hours</div>
                  </div>
                </div>
              </a>
            </div>

            <!-- Quick Benefits -->
            <div class="space-y-2 mt-6">
              <div class="flex items-center space-x-3">
                <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
                <span class="text-white font-medium text-sm">Available 7 days a week</span>
              </div>
              <div class="flex items-center space-x-3">
                <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
                <span class="text-white font-medium text-sm">Response within 24 hours</span>
              </div>
              <div class="flex items-center space-x-3">
                <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 20 20">
                  <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                </svg>
                <span class="text-white font-medium text-sm">100% free consultation</span>
              </div>
            </div>
          </div>

          <!-- Right Column: Enhanced Contact Form -->
          <div class="animate-on-scroll">
            <div class="bg-white/10 backdrop-blur-xl rounded-3xl border border-white/20 shadow-2xl p-8">
              <div class="text-center mb-6">
                <h2 class="text-2xl font-bold text-white mb-2">Tell Us What You Need</h2>
                <p class="text-white/80 text-sm">We'll send you qualified warehouse options within 24 hours.</p>
              </div>
              
              <form 
                name="warehouse-contact" 
                method="POST" 
                data-netlify="true" 
                netlify-honeypot="bot-field"
                action="/success"
                class="space-y-4"
              >
                <!-- Hidden fields -->
                <input type="hidden" name="form-name" value="warehouse-contact" />
                <input type="hidden" name="source" value="contact-page-hero" />
                <div class="hidden">
                  <input name="bot-field" />
                </div>

                <!-- Name, Email Row -->
                <div class="grid grid-cols-2 gap-3">
                  <div>
                    <label class="block text-white/90 text-sm font-medium mb-2">Name *</label>
                    <input 
                      type="text" 
                      name="name" 
                      required 
                      class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300"
                      placeholder="Your Name"
                    />
                  </div>
                  <div>
                    <label class="block text-white/90 text-sm font-medium mb-2">Email *</label>
                    <input 
                      type="email" 
                      name="email" 
                      required 
                      class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300"
                      placeholder="your@email.com"
                    />
                  </div>
                </div>

                <!-- Phone, Company Row -->
                <div class="grid grid-cols-2 gap-3">
                  <div>
                    <label class="block text-white/90 text-sm font-medium mb-2">Phone *</label>
                    <input 
                      type="tel" 
                      name="phone" 
                      required
                      class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300"
                      placeholder="(954) 123-4567"
                    />
                  </div>
                  <div>
                    <label class="block text-white/90 text-sm font-medium mb-2">Company</label>
                    <input 
                      type="text" 
                      name="company" 
                      class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300"
                      placeholder="Company Name"
                    />
                  </div>
                </div>

                <!-- Size, Warehouse Row -->
                <div class="grid grid-cols-2 gap-3">
                  <div>
                    <label class="block text-white/90 text-sm font-medium mb-2">Size (SF)</label>
                    <input 
                      type="text" 
                      name="size-needed" 
                      class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300"
                      placeholder="10,000"
                    />
                  </div>
                  <div>
                    <label class="block text-white/90 text-sm font-medium mb-2">Property *</label>
                    <select 
                      name="warehouse-interested" 
                      required
                      class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300"
                    >
                      <option value="" class="text-gray-900">Select...</option>
                      <option value="any" class="text-gray-900">Any Available</option>
                      <option value="warehouse" class="text-gray-900">Warehouse</option>
                      <option value="manufacturing" class="text-gray-900">Manufacturing</option>
                      <option value="distribution" class="text-gray-900">Distribution</option>
                      <option value="flex-space" class="text-gray-900">Flex Space</option>
                      <option value="cold-storage" class="text-gray-900">Cold Storage</option>
                      <option value="land" class="text-gray-900">Land</option>
                      {allWarehouses.length > 0 && (
                        <optgroup label="Specific Properties" class="text-gray-900">
                          {allWarehouses.map(warehouse => (
                            <option value={warehouse.slug} class="text-gray-900">{warehouse.title}</option>
                          ))}
                        </optgroup>
                      )}
                    </select>
                  </div>
                </div>

                <!-- Requirements -->
                <div>
                  <label class="block text-white/90 text-sm font-medium mb-2">Requirements & Details</label>
                  <textarea 
                    name="requirements" 
                    rows="4"
                    class="w-full px-4 py-3 rounded-xl border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-2 focus:ring-primary-400/20 transition-all duration-300 resize-none"
                    placeholder="Loading docks, clear height, office space, budget range, preferred locations, timeline, special requirements..."
                  ></textarea>
                </div>

                <!-- Submit Button -->
                <button 
                  type="submit" 
                  class="w-full bg-gradient-to-r from-primary-600 to-primary-700 text-white px-6 py-4 rounded-xl text-lg font-bold hover:from-primary-700 hover:to-primary-800 hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 transform focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 focus:ring-offset-transparent"
                >
                  Get Matched With Warehouses
                </button>

                <p class="text-sm text-white/60 text-center">
                  Free service • Landlords pay our fees • No obligation
                </p>
              </form>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Why Choose Us Section -->
    <section class="py-20 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="grid lg:grid-cols-2 gap-16 items-center">
          
          <!-- Left Column: Content -->
          <div class="animate-on-scroll">
            <h2 class="text-3xl font-bold text-gray-900 mb-8">
              Why We're Different
            </h2>
            
            <div class="space-y-4">
              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
                <div>
                  <p class="font-semibold text-gray-900">Completely Free to You</p>
                  <p class="text-sm text-gray-600">Landlords pay all of our fees—100% free tenant representation</p>
                </div>
              </div>

              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
                <div>
                  <p class="font-semibold text-gray-900">Industrial Specialists</p>
                  <p class="text-sm text-gray-600">We focus exclusively on warehouse and industrial properties</p>
                </div>
              </div>

              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
                <div>
                  <p class="font-semibold text-gray-900">Deep Market Knowledge</p>
                  <p class="text-sm text-gray-600">Extensive experience across Miami-Dade, Broward, and Palm Beach</p>
                </div>
              </div>

              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
                <div>
                  <p class="font-semibold text-gray-900">Fast Results</p>
                  <p class="text-sm text-gray-600">Efficient process gets you touring qualified properties quickly</p>
                </div>
              </div>

              <div class="flex items-start space-x-3">
                <div class="w-6 h-6 bg-green-500 rounded-full flex items-center justify-center flex-shrink-0 mt-0.5">
                  <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                  </svg>
                </div>
                <div>
                  <p class="font-semibold text-gray-900">Expert Negotiation</p>
                  <p class="text-sm text-gray-600">We negotiate terms that protect your interests and save you money</p>
                </div>
              </div>
            </div>
          </div>

          <!-- Right Column: Image/Graphic -->
          <div class="animate-on-scroll">
            <div class="bg-gradient-to-br from-primary-50 to-blue-50 rounded-3xl p-8 text-center">
              <div class="w-24 h-24 bg-primary-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
                <svg class="w-12 h-12 text-primary-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 21V5a2 2 0 00-2-2H7a2 2 0 00-2 2v16m14 0h2m-2 0h-5m-9 0H3m2 0h5M9 7h1m-1 4h1m4-4h1m-1 4h1m-5 10v-5a1 1 0 011-1h2a1 1 0 011 1v5m-4 0h4"/>
                </svg>
              </div>
              <h3 class="text-xl font-bold text-gray-900 mb-4">
                From 500 SF to 500,000+ SF
              </h3>
              <p class="text-gray-600 mb-6">
                Whether you need a small flex space or a massive distribution center, we have the network and expertise to find your perfect match.
              </p>
              <div class="text-2xl font-bold text-primary-600">
                24-Hour Response Guarantee
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Process Section -->
    <section class="py-20 bg-gray-50">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-16">
          <h2 class="text-4xl font-bold text-gray-900 mb-4">Our Simple 3-Step Process</h2>
          <p class="text-xl text-gray-600 max-w-2xl mx-auto">
            Getting matched with your perfect warehouse space has never been easier
          </p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
          <!-- Step 1 -->
          <div class="text-center animate-on-scroll">
            <div class="w-16 h-16 bg-primary-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
              <span class="text-2xl font-bold text-primary-600">1</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-4">Tell Us Your Needs</h3>
            <p class="text-gray-600">
              Share your space requirements, budget, timeline, and any special features you need. The more details, the better we can help.
            </p>
          </div>

          <!-- Step 2 -->
          <div class="text-center animate-on-scroll">
            <div class="w-16 h-16 bg-primary-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
              <span class="text-2xl font-bold text-primary-600">2</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-4">We Find Matches</h3>
            <p class="text-gray-600">
              Within 24 hours, we'll send you a curated list of qualified warehouse options that fit your criteria, complete with photos and details.
            </p>
          </div>

          <!-- Step 3 -->
          <div class="text-center animate-on-scroll">
            <div class="w-16 h-16 bg-primary-100 rounded-2xl flex items-center justify-center mx-auto mb-6">
              <span class="text-2xl font-bold text-primary-600">3</span>
            </div>
            <h3 class="text-xl font-bold text-gray-900 mb-4">Tour & Negotiate</h3>
            <p class="text-gray-600">
              We'll schedule tours of your top choices and handle all negotiations to secure the best terms for your business.
            </p>
          </div>
        </div>

        <!-- CTA -->
        <div class="text-center mt-16">
          <a 
            href="#" 
            onclick="document.querySelector('form').scrollIntoView({ behavior: 'smooth' }); return false;"
            class="bg-gradient-to-r from-primary-600 to-primary-700 text-white px-8 py-4 rounded-2xl text-lg font-semibold hover:from-primary-700 hover:to-primary-800 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 transform inline-block"
          >
            Start Your Search Today
          </a>
          <p class="text-gray-500 text-sm mt-4">
            No cost, no retainer, no obligation
          </p>
        </div>
      </div>
    </section>

    <!-- Final CTA -->
    <section class="py-20 bg-gradient-to-br from-gray-900 to-gray-800">
      <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 text-center">
        <h2 class="text-4xl font-bold text-white mb-6">
          Ready to Find Your Perfect Warehouse?
        </h2>
        <p class="text-xl text-gray-300 mb-8 max-w-2xl mx-auto">
          Join hundreds of satisfied businesses who found their ideal industrial space through our free tenant representation service.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-4 justify-center mb-8">
          <a 
            href="tel:954-937-9667" 
            class="bg-gradient-to-r from-primary-600 to-primary-700 text-white px-8 py-4 rounded-2xl text-lg font-semibold hover:from-primary-700 hover:to-primary-800 hover:shadow-xl hover:-translate-y-1 transition-all duration-300 transform"
          >
            Call (954) 937-9667 Now
          </a>
          <a 
            href="/properties" 
            class="bg-white/10 backdrop-blur-sm text-white border-2 border-white/30 px-8 py-4 rounded-2xl text-lg font-semibold hover:bg-white/20 hover:border-white/50 transition-all duration-300"
          >
            Browse Available Properties
          </a>
        </div>
        
        <p class="text-gray-400 text-sm">
          Available 7 days a week • Free consultation • Landlords pay our fees
        </p>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  .animate-on-scroll {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
  }

  .animate-on-scroll.animated {
    opacity: 1;
    transform: translateY(0);
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-reverse {
    animation: float 6s ease-in-out infinite reverse;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(1deg); }
  }

  .bg-pattern-dots {
    background-image: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  /* Smooth scroll for anchor links */
  html {
    scroll-behavior: smooth;
  }
</style>

<script>
  // Scroll-triggered animations
  const animateOnScroll = () => {
    const elements = document.querySelectorAll('.animate-on-scroll');
    
    elements.forEach(element => {
      const elementTop = element.getBoundingClientRect().top;
      const elementVisible = 150;
      
      if (elementTop < window.innerHeight - elementVisible) {
        element.classList.add('animated');
      }
    });
  };

  // Run on page load and scroll
  window.addEventListener('scroll', animateOnScroll);
  window.addEventListener('load', animateOnScroll);
  
  // Initial check
  animateOnScroll();
</script>