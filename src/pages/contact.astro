---
// Updated contact page with perfectly centered hero content for mobile
import BaseLayout from '../layouts/BaseLayout.astro';
import Navigation from '../components/Navigation.astro';
import Footer from '../components/Footer.astro';
import { getCollection } from 'astro:content';

// Define types for warehouse options
interface WarehouseOption {
  title: string;
  slug: string;
}

// Get warehouses for the form dropdown
let allWarehouses: WarehouseOption[] = [];
try {
  const warehouseCollection = await getCollection('warehouses');
  allWarehouses = warehouseCollection
    .filter(w => w.data.availability === 'available')
    .map(w => ({ title: w.data.title, slug: w.data.slug }));
} catch (error) {
  console.log('Could not load warehouses for dropdown');
}

// Form dropdown options (matching ContactModal exactly)
const counties = [
  'Miami-Dade County',
  'Broward County', 
  'Palm Beach County'
];

const priceRanges = [
  '$3-5 per SF',
  '$5-7 per SF',
  '$7-10 per SF',
  '$10-15 per SF',
  '$15+ per SF',
  'Negotiable'
];

const sqftRanges = [
  '500-2,000 SF',
  '2,000-5,000 SF',
  '5,000-10,000 SF',
  '10,000-25,000 SF',
  '25,000-50,000 SF',
  '50,000-100,000 SF',
  '100,000+ SF'
];
---

<BaseLayout 
  title="Contact Warehouse Locating | Get Matched With Warehouses Today"
  description="Tell us what you need. We'll send qualified options and set up tours. Free tenant representation across South Florida."
>
  <Navigation />
  
  <main>
    <!-- Hero Section - Full Screen -->
    <section class="hero-section relative bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900 overflow-hidden flex items-center">
      <!-- Background Pattern -->
      <div class="absolute inset-0">
        <div class="absolute top-1/4 left-1/4 w-96 h-96 bg-blue-500/10 rounded-full blur-3xl animate-float"></div>
        <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-cyan-500/10 rounded-full blur-3xl animate-float-reverse"></div>
        <div class="absolute inset-0 bg-pattern-dots opacity-10"></div>
      </div>
      
      <!-- Mobile Layout -->
      <div class="md:hidden relative z-10 h-full w-full flex items-center">
        <div class="max-w-sm mx-auto text-center space-y-8">
          
          <!-- Headline -->
          <div class="mt-8">
            <h1 class="text-3xl font-black text-white leading-tight mb-4">
              FIND YOUR
              <span class="block bg-gradient-to-r from-primary-400 to-blue-400 bg-clip-text text-transparent">
                WAREHOUSE TODAY
              </span>
            </h1>
            
            <p class="text-base text-gray-300 leading-relaxed mb-3">
              Tell us what you need. We'll send qualified options and set up tours.
            </p>

            <p class="text-sm text-gray-400 leading-relaxed">
              <span class="text-white font-semibold">Completely free service.</span> Our fee is paid by landlords.
            </p>
          </div>

          <!-- Contact Cards -->
          <div class="space-y-4">
            <!-- Phone Card -->
            <a 
              href="tel:954-937-9667" 
              class="block p-4 bg-white/10 backdrop-blur-xl rounded-2xl border border-white/20 shadow-xl"
            >
              <div class="flex items-center space-x-3">
                <div class="w-10 h-10 bg-green-500/20 rounded-xl flex items-center justify-center">
                  <svg class="w-5 h-5 text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                  </svg>
                </div>
                <div class="text-left">
                  <div class="text-xs font-semibold text-white/80">Call Us Direct</div>
                  <div class="text-lg font-black text-white">(954) 937-9667</div>
                  <div class="text-xs text-white/60">Available 7 days</div>
                </div>
              </div>
            </a>

            <!-- Email Card -->
            <a 
              href="mailto:info@warehouselocating.com" 
              class="block p-4 bg-white/10 backdrop-blur-xl rounded-2xl border border-white/20 shadow-xl"
            >
              <div class="flex items-center space-x-3">
                <div class="w-10 h-10 bg-blue-500/20 rounded-xl flex items-center justify-center">
                  <svg class="w-5 h-5 text-blue-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                  </svg>
                </div>
                <div class="text-left min-w-0 flex-1">
                  <div class="text-xs font-semibold text-white/80">Email Us</div>
                  <div class="text-sm font-black text-white truncate">info@warehouselocating.com</div>
                  <div class="text-xs text-white/60">Fast response</div>
                </div>
              </div>
            </a>
          </div>

          <!-- Contact Form -->
          <div class="bg-white/10 backdrop-blur-xl rounded-2xl border border-white/20 shadow-xl p-4">
            <div class="text-center mb-4">
              <h2 class="text-lg font-bold text-white mb-1">Tell Us What You Need</h2>
              <p class="text-white/80 text-xs">We'll send you qualified warehouse options within 24 hours.</p>
            </div>
            
            <form 
              name="warehouse-inquiry-modal" 
              method="POST" 
              data-netlify="true" 
              netlify-honeypot="bot-field"
              action="/success"
              class="space-y-3"
            >
              <!-- Hidden fields -->
              <input type="hidden" name="form-name" value="warehouse-inquiry-modal" />
              <input type="hidden" name="source" value="contact-page-hero-mobile" />
              <div class="hidden">
                <input name="bot-field" />
              </div>

              <!-- Name & Email Row -->
              <div class="grid grid-cols-2 gap-2">
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Name *</label>
                  <input 
                    type="text" 
                    name="name" 
                    required 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                    placeholder="Your Name"
                  />
                </div>
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Email *</label>
                  <input 
                    type="email" 
                    name="email" 
                    required 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                    placeholder="your@email.com"
                  />
                </div>
              </div>

              <!-- Phone & Company Row -->
              <div class="grid grid-cols-2 gap-2">
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Phone</label>
                  <input 
                    type="tel" 
                    name="phone" 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                    placeholder="(555) 123-4567"
                  />
                </div>
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Company</label>
                  <input 
                    type="text" 
                    name="company" 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                    placeholder="Company Name"
                  />
                </div>
              </div>

              <!-- Property Interest & County Row -->
              <div class="grid grid-cols-2 gap-2">
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Property Interest *</label>
                  <select 
                    name="property-interest" 
                    required
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                  >
                    <option value="" class="bg-gray-800">Select property...</option>
                    <option value="any" class="bg-gray-800">Any Available Property</option>
                    {allWarehouses.map(warehouse => (
                      <option value={warehouse.slug} class="bg-gray-800">
                        {warehouse.title}
                      </option>
                    ))}
                  </select>
                </div>
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">County</label>
                  <select 
                    name="county" 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                  >
                    <option value="" class="bg-gray-800">Select county...</option>
                    <option value="any" class="bg-gray-800">Any County</option>
                    {counties.map(county => (
                      <option value={county} class="bg-gray-800">{county}</option>
                    ))}
                  </select>
                </div>
              </div>

              <!-- Size & Price Range Row -->
              <div class="grid grid-cols-2 gap-2">
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Size (SF)</label>
                  <select 
                    name="size-needed" 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                  >
                    <option value="" class="bg-gray-800">Select size...</option>
                    {sqftRanges.map(range => (
                      <option value={range} class="bg-gray-800">{range}</option>
                    ))}
                  </select>
                </div>
                <div>
                  <label class="block text-white/90 text-xs font-medium mb-1">Price Range</label>
                  <select 
                    name="price-range" 
                    class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                  >
                    <option value="" class="bg-gray-800">Select price...</option>
                    {priceRanges.map(range => (
                      <option value={range} class="bg-gray-800">{range}</option>
                    ))}
                  </select>
                </div>
              </div>

              <!-- Timing -->
              <div>
                <label class="block text-white/90 text-xs font-medium mb-1">Timing</label>
                <select 
                  name="timing" 
                  class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                >
                  <option value="" class="bg-gray-800">Select timeline...</option>
                  <option value="immediate" class="bg-gray-800">Immediate (0-30 days)</option>
                  <option value="1-3-months" class="bg-gray-800">1-3 months</option>
                  <option value="3-6-months" class="bg-gray-800">3-6 months</option>
                  <option value="6-12-months" class="bg-gray-800">6-12 months</option>
                  <option value="12-months-plus" class="bg-gray-800">12+ months</option>
                  <option value="just-exploring" class="bg-gray-800">Just exploring options</option>
                </select>
              </div>

              <!-- Requirements & Message -->
              <div>
                <label class="block text-white/90 text-xs font-medium mb-1">Requirements & Message</label>
                <textarea 
                  name="requirements" 
                  rows="2"
                  class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 resize-vertical text-sm"
                  placeholder="Loading docks, clear height, special features, budget, location preferences..."
                ></textarea>
              </div>

              <!-- Submit Button -->
              <button 
                type="submit" 
                class="w-full bg-gradient-to-r from-primary-600 to-primary-700 text-white px-4 py-3 rounded-lg text-sm font-bold hover:from-primary-700 hover:to-primary-800 hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 transform focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 focus:ring-offset-transparent"
              >
                Get Matched With Warehouses
              </button>

              <p class="text-xs text-white/60 text-center">
                Free service • Landlords pay our fees • No obligation
              </p>
            </form>
          </div>

          <!-- Value Prop -->
          <div class="flex items-center justify-center space-x-2">
            <svg class="w-4 h-4 text-green-400" fill="currentColor" viewBox="0 0 20 20">
              <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
            </svg>
            <span class="text-white font-medium text-sm">100% free consultation</span>
          </div>
        </div>
      </div>

      <!-- Desktop Hero Layout - TWO COLUMN WITH CONTACT -->
      <div class="hidden md:block relative z-10 w-full h-full flex items-center">
        <div class="max-w-7xl mx-auto w-full h-full flex items-center">
          <div class="grid md:grid-cols-2 gap-8 lg:gap-12 xl:gap-16 2xl:gap-20 items-center w-full">
            
            <!-- Left Column: Hero Content -->
            <div class="text-left">
              <h1 class="text-3xl md:text-4xl lg:text-5xl xl:text-5xl 2xl:text-6xl font-black text-white leading-tight mb-3 lg:mb-4 xl:mb-5 2xl:mb-6">
                FIND YOUR
                <span class="block bg-gradient-to-r from-primary-400 to-blue-400 bg-clip-text text-transparent">
                  WAREHOUSE TODAY
                </span>
              </h1>
              
              <p class="text-base md:text-lg lg:text-xl xl:text-xl 2xl:text-2xl text-gray-300 mb-2 lg:mb-3 xl:mb-4 2xl:mb-5">
                Tell us what you need. We'll send qualified options and set up tours.
              </p>

              <p class="text-sm md:text-base lg:text-lg xl:text-lg 2xl:text-xl text-gray-400 mb-4 lg:mb-5 xl:mb-6 2xl:mb-7">
                <span class="text-white font-semibold">Completely free service.</span> Our fee is paid by landlords.
              </p>

              <!-- Value Props -->
              <div class="space-y-2 lg:space-y-2 xl:space-y-3 2xl:space-y-3 mb-4 lg:mb-5 xl:mb-6 2xl:mb-7">
                <div class="flex items-center space-x-3">
                  <svg class="w-4 h-4 lg:w-5 lg:h-5 xl:w-5 xl:h-5 2xl:w-6 2xl:h-6 text-green-400 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  <span class="text-white font-medium text-sm lg:text-base xl:text-base 2xl:text-lg">100% free service—no cost, no retainer, no obligation</span>
                </div>
                <div class="flex items-center space-x-3">
                  <svg class="w-4 h-4 lg:w-5 lg:h-5 xl:w-5 xl:h-5 2xl:w-6 2xl:h-6 text-green-400 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  <span class="text-white font-medium text-sm lg:text-base xl:text-base 2xl:text-lg">Industrial experts—warehouses only, deep market knowledge</span>
                </div>
                <div class="flex items-center space-x-3">
                  <svg class="w-4 h-4 lg:w-5 lg:h-5 xl:w-5 xl:h-5 2xl:w-6 2xl:h-6 text-green-400 flex-shrink-0" fill="currentColor" viewBox="0 0 20 20">
                    <path fill-rule="evenodd" d="M16.707 5.293a1 1 0 010 1.414l-8 8a1 1 0 01-1.414 0l-4-4a1 1 0 011.414-1.414L8 12.586l7.293-7.293a1 1 0 011.414 0z" clip-rule="evenodd"/>
                  </svg>
                  <span class="text-white font-medium text-sm lg:text-base xl:text-base 2xl:text-lg">Fast results—qualified options within 24 hours</span>
                </div>
              </div>

              <!-- Action Buttons -->
              <div class="flex flex-col sm:flex-row gap-3 lg:gap-3 xl:gap-4 2xl:gap-4">
                <a 
                  href="tel:954-937-9667" 
                  class="bg-gradient-to-r from-primary-600 to-primary-700 text-white px-6 lg:px-7 xl:px-8 2xl:px-9 py-3 lg:py-3 xl:py-4 2xl:py-4 rounded-2xl text-base lg:text-lg xl:text-lg 2xl:text-xl font-bold hover:from-primary-700 hover:to-primary-800 hover:shadow-2xl hover:-translate-y-1 transition-all duration-300 transform text-center"
                >
                  Call (954) 937-9667
                </a>
                <a 
                  href="mailto:info@warehouselocating.com" 
                  class="bg-white/10 backdrop-blur-sm text-white border-2 border-white/30 px-6 lg:px-7 xl:px-8 2xl:px-9 py-3 lg:py-3 xl:py-4 2xl:py-4 rounded-2xl text-base lg:text-lg xl:text-lg 2xl:text-xl font-bold hover:bg-white/20 hover:border-white/50 transition-all duration-300 text-center whitespace-nowrap"
                >
                  Email Us
                </a>
              </div>
            </div>

            <!-- Right Column: Contact Form -->
            <div class="flex justify-end items-center h-full">
              <div class="bg-white/10 backdrop-blur-xl rounded-2xl border border-white/20 p-4 lg:p-5 xl:p-6 2xl:p-7 w-full max-w-sm lg:max-w-md xl:max-w-lg 2xl:max-w-lg my-8 lg:my-12 xl:my-16 2xl:my-20">
                <div class="text-center mb-3 lg:mb-4 xl:mb-5 2xl:mb-6">
                  <h2 class="text-lg lg:text-xl xl:text-xl 2xl:text-2xl font-bold text-white mb-1 lg:mb-2">Find Your Perfect Warehouse Space</h2>
                  <p class="text-white/80 text-xs lg:text-sm xl:text-sm 2xl:text-base">We'll get back to you within 24 hours with qualified options.</p>
                </div>
              
                <form 
                  name="warehouse-inquiry-hero" 
                  method="POST" 
                  data-netlify="true" 
                  netlify-honeypot="bot-field"
                  action="/success"
                  class="space-y-3 lg:space-y-3 xl:space-y-4 2xl:space-y-4"
                >
                  <!-- Hidden fields -->
                  <input type="hidden" name="form-name" value="warehouse-inquiry-hero" />
                  <input type="hidden" name="source" value="contact-page-hero-desktop" />
                  <div class="hidden">
                    <input name="bot-field" />
                  </div>

                  <!-- Name & Email Row -->
                  <div class="grid grid-cols-2 gap-3 xl:gap-4 2xl:gap-5">
                    <div>
                      <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Name *</label>
                      <input 
                        type="text" 
                        name="name" 
                        required 
                        class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                        placeholder="Your Name"
                      />
                    </div>
                    <div>
                      <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Email *</label>
                      <input 
                        type="email" 
                        name="email" 
                        required 
                        class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                        placeholder="your@email.com"
                      />
                    </div>
                  </div>

                  <!-- Phone & Company Row -->
                  <div class="grid grid-cols-2 gap-3 xl:gap-4 2xl:gap-5">
                    <div>
                      <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Phone</label>
                      <input 
                        type="tel" 
                        name="phone" 
                        class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                        placeholder="(555) 123-4567"
                      />
                    </div>
                    <div>
                      <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Company</label>
                      <input 
                        type="text" 
                        name="company" 
                        class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                        placeholder="Company Name"
                      />
                    </div>
                  </div>

                  <!-- Property Interest -->
                  <div>
                    <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Property Interest *</label>
                    <select 
                      name="property-interest" 
                      required
                      class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                    >
                      <option value="" class="bg-gray-800">Select property...</option>
                      <option value="any" class="bg-gray-800">Any Available Property</option>
                      {allWarehouses.map(warehouse => (
                        <option value={warehouse.slug} class="bg-gray-800">
                          {warehouse.title}
                        </option>
                      ))}
                    </select>
                  </div>

                  <!-- County & Size Row -->
                  <div class="grid grid-cols-2 gap-3 xl:gap-4 2xl:gap-5">
                    <div>
                      <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">County</label>
                      <select 
                        name="county" 
                        class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                      >
                        <option value="" class="bg-gray-800">Select county...</option>
                        <option value="any" class="bg-gray-800">Any County</option>
                        {counties.map(county => (
                          <option value={county} class="bg-gray-800">{county}</option>
                        ))}
                      </select>
                    </div>
                    <div>
                      <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Size (SF)</label>
                      <select 
                        name="size-needed" 
                        class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 text-sm"
                      >
                        <option value="" class="bg-gray-800">Select size...</option>
                        {sqftRanges.map(range => (
                          <option value={range} class="bg-gray-800">{range}</option>
                        ))}
                      </select>
                    </div>
                  </div>

                  <!-- Requirements & Message -->
                  <div>
                    <label class="block text-white/90 text-xs xl:text-sm 2xl:text-base font-medium mb-1 xl:mb-2">Requirements</label>
                    <textarea 
                      name="requirements" 
                      rows="3"
                      class="w-full px-3 py-2 rounded-lg border border-white/20 bg-white/10 backdrop-blur-sm text-white placeholder-white/60 focus:border-primary-400 focus:ring-1 focus:ring-primary-400/20 transition-all duration-300 resize-vertical text-sm"
                      placeholder="Loading docks, clear height, special features, location preferences..."
                    ></textarea>
                  </div>

                  <!-- Submit Button -->
                  <button 
                    type="submit" 
                    class="w-full bg-gradient-to-r from-primary-600 to-primary-700 text-white px-4 xl:px-6 2xl:px-8 py-3 xl:py-4 2xl:py-5 rounded-lg text-sm xl:text-base 2xl:text-lg font-bold hover:from-primary-700 hover:to-primary-800 hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 transform focus:outline-none focus:ring-2 focus:ring-primary-500 focus:ring-offset-2 focus:ring-offset-transparent"
                  >
                    Get Free Consultation
                  </button>

                  <p class="text-xs xl:text-sm 2xl:text-base text-white/60 text-center">
                    Free service • Landlords pay our fees • No obligation
                  </p>
                </form>
              </div>
            </div>
          </div>
        </div>
      </div>
    </section>

    <!-- Contact Information Section -->
    <section class="py-16 bg-white">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl font-bold text-gray-900 mb-4">Multiple Ways to Connect</h2>
          <p class="text-lg text-gray-600">Choose the communication method that works best for you</p>
        </div>

        <div class="grid md:grid-cols-3 gap-8">
          <!-- Phone -->
          <a 
            href="tel:954-937-9667"
            class="group text-center p-6 bg-gray-50 rounded-2xl hover:bg-gray-100 transition-all duration-200 hover:scale-105 hover:shadow-lg cursor-pointer"
          >
            <div class="w-16 h-16 bg-green-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:bg-green-200 transition-colors duration-200">
              <svg class="w-8 h-8 text-green-600 group-hover:text-green-700 transition-colors duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2 group-hover:text-green-700 transition-colors duration-200">Call Direct</h3>
            <div class="text-green-600 hover:text-green-700 font-medium text-lg group-hover:text-green-800 transition-colors duration-200">
              (954) 937-9667
            </div>
            <p class="text-gray-500 text-sm mt-1 group-hover:text-gray-600 transition-colors duration-200">Available 7 days a week</p>
          </a>

          <!-- Email -->
          <a 
            href="mailto:info@warehouselocating.com"
            class="group text-center p-6 bg-gray-50 rounded-2xl hover:bg-gray-100 transition-all duration-200 hover:scale-105 hover:shadow-lg cursor-pointer"
          >
            <div class="w-16 h-16 bg-blue-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:bg-blue-200 transition-colors duration-200">
              <svg class="w-8 h-8 text-blue-600 group-hover:text-blue-700 transition-colors duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2 group-hover:text-blue-700 transition-colors duration-200">Email Us</h3>
            <div class="text-blue-600 hover:text-blue-700 font-medium group-hover:text-blue-800 transition-colors duration-200">
              info@warehouselocating.com
            </div>
            <p class="text-gray-500 text-sm mt-1 group-hover:text-gray-600 transition-colors duration-200">Fast response guaranteed</p>
          </a>

          <!-- Form -->
          <button 
            onclick="openContactModal('multiple-ways-form')"
            class="group text-center p-6 bg-gray-50 rounded-2xl hover:bg-gray-100 transition-all duration-200 hover:scale-105 hover:shadow-lg cursor-pointer w-full"
          >
            <div class="w-16 h-16 bg-primary-100 rounded-2xl flex items-center justify-center mx-auto mb-4 group-hover:bg-primary-200 transition-colors duration-200">
              <svg class="w-8 h-8 text-primary-600 group-hover:text-primary-700 transition-colors duration-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
              </svg>
            </div>
            <h3 class="text-lg font-semibold text-gray-900 mb-2 group-hover:text-primary-700 transition-colors duration-200">Fill Out Form</h3>
            <div class="text-primary-600 hover:text-primary-700 font-medium group-hover:text-primary-800 transition-colors duration-200">
              Get Matched Today
            </div>
            <p class="text-gray-500 text-sm mt-1 group-hover:text-gray-600 transition-colors duration-200">Quick form with all your requirements</p>
          </button>
        </div>
      </div>
    </section>

    <!-- FAQ Section -->
    <section class="relative py-16 overflow-hidden">
      <!-- Background Image -->
      <div class="absolute inset-0">
        <img 
          src="/images/uploads/wareint.png" 
          alt="Warehouse interior" 
          class="w-full h-full object-cover"
        />
        <div class="absolute inset-0 bg-gray-900/80"></div>
      </div>
      
      <div class="relative z-10 max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="text-center mb-12">
          <h2 class="text-3xl font-bold text-white mb-4">Frequently Asked Questions</h2>
          <p class="text-lg text-gray-300">Quick answers to common questions about our service</p>
        </div>

        <div class="space-y-6">
          <div class="bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 shadow-xl">
            <h3 class="text-lg font-semibold text-white mb-3">How much does your service cost?</h3>
            <p class="text-gray-300">Our service is completely free to tenants. Landlords pay all brokerage fees, so you get expert representation at zero cost.</p>
          </div>

          <div class="bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 shadow-xl">
            <h3 class="text-lg font-semibold text-white mb-3">How quickly can you find options?</h3>
            <p class="text-gray-300">We typically provide qualified warehouse options within 24 hours of receiving your requirements. Tours can usually be scheduled within 2-3 business days.</p>
          </div>

          <div class="bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 shadow-xl">
            <h3 class="text-lg font-semibold text-white mb-3">What areas do you cover?</h3>
            <p class="text-gray-300">We specialize in industrial properties throughout South Florida, including Miami-Dade, Broward, and Palm Beach counties.</p>
          </div>

          <div class="bg-white/10 backdrop-blur-xl rounded-2xl p-6 border border-white/20 shadow-xl">
            <h3 class="text-lg font-semibold text-white mb-3">Do you handle lease negotiations?</h3>
            <p class="text-gray-300">Yes, we handle all lease negotiations on your behalf, ensuring you get the best possible terms and protecting your interests throughout the process.</p>
          </div>
        </div>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>

<style>
  /* Dynamic viewport-based hero scaling - adapts to ANY screen size */
  .hero-section {
    --header-height: 60px;
    --base-padding-y: clamp(1rem, max(4vw, 3vh), 6rem);
    --base-padding-x: clamp(1rem, 3vw, 4rem);
    
    height: calc(100vh - var(--header-height));
    height: calc(100dvh - var(--header-height));
    padding: var(--base-padding-y) var(--base-padding-x);
    
    /* Dynamic content scaling based on viewport */
    --content-scale: clamp(0.8, 1.2vw + 0.5rem, 1.2);
  }
  
  /* Mobile optimization */
  @media (max-width: 768px) {
    .hero-section {
      --header-height: 50px;
      --base-padding-y: clamp(1.5rem, max(6vw, 4vh), 3rem);
      --base-padding-x: clamp(1rem, 4vw, 2rem);
    }
  }
  
  /* Tablet optimization */
  @media (min-width: 769px) and (max-width: 1024px) {
    .hero-section {
      --header-height: 65px;
      --base-padding-y: clamp(2rem, max(5vw, 4vh), 4rem);
      --base-padding-x: clamp(1.5rem, 3vw, 3rem);
    }
  }
  
  /* Desktop optimization */
  @media (min-width: 1025px) {
    .hero-section {
      --header-height: 70px;
      --base-padding-y: clamp(2.5rem, max(4vw, 3vh), 6rem);
      --base-padding-x: clamp(2rem, 2.5vw, 4rem);
    }
  }
  
  /* Ultra-wide optimization */
  @media (min-width: 1920px) {
    .hero-section {
      --base-padding-y: clamp(3rem, max(3vw, 2vh), 8rem);
      --base-padding-x: clamp(3rem, 2vw, 6rem);
    }
  }
  
  /* Height-specific adjustments for very short viewports */
  @media (max-height: 600px) {
    .hero-section {
      --base-padding-y: clamp(1rem, max(2vw, 2vh), 3rem);
    }
  }
  
  @media (max-height: 500px) {
    .hero-section {
      --base-padding-y: clamp(0.75rem, max(1.5vw, 1.5vh), 2rem);
    }
  }
  
  /* Ensure perfect scaling on very narrow or very wide screens */
  @media (aspect-ratio < 1/1) {
    .hero-section {
      --base-padding-y: clamp(1rem, 8vh, 4rem);
    }
  }
  
  @media (aspect-ratio > 21/9) {
    .hero-section {
      --base-padding-x: clamp(4rem, 10vw, 12rem);
    }
  }
  
  /* Form container height constraints */
  .hero-section .bg-white\/10 {
    max-height: calc(100vh - var(--header-height) - 2 * var(--base-padding-y));
    max-height: calc(100dvh - var(--header-height) - 2 * var(--base-padding-y));
    overflow-y: auto;
    height: fit-content;
    display: flex;
    flex-direction: column;
  }
  
  /* Ensure form fits on very short screens */
  @media (max-height: 600px) {
    .hero-section .bg-white\/10 {
      max-height: calc(100vh - var(--header-height) - 2rem);
      max-height: calc(100dvh - var(--header-height) - 2rem);
    }
  }
  
  @media (max-height: 500px) {
    .hero-section .bg-white\/10 {
      max-height: calc(100vh - var(--header-height) - 1.5rem);
      max-height: calc(100dvh - var(--header-height) - 1.5rem);
    }
  }

  .animate-on-scroll {
    opacity: 0;
    transform: translateY(30px);
    transition: all 0.8s ease-out;
  }

  .animate-on-scroll.animated {
    opacity: 1;
    transform: translateY(0);
  }

  .animate-float {
    animation: float 6s ease-in-out infinite;
  }

  .animate-float-reverse {
    animation: float 6s ease-in-out infinite reverse;
  }

  @keyframes float {
    0%, 100% { transform: translateY(0px) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(1deg); }
  }

  .bg-pattern-dots {
    background-image: radial-gradient(circle, rgba(255,255,255,0.1) 1px, transparent 1px);
    background-size: 20px 20px;
  }
</style>

<script>
  // Scroll-triggered animations
  const animateOnScroll = () => {
    const elements = document.querySelectorAll('.animate-on-scroll');
    
    elements.forEach(element => {
      const elementTop = element.getBoundingClientRect().top;
      const elementVisible = 150;
      
      if (elementTop < window.innerHeight - elementVisible) {
        element.classList.add('animated');
      }
    });
  };

  // Run on page load and scroll
  window.addEventListener('scroll', animateOnScroll);
  window.addEventListener('load', animateOnScroll);
  
  // Initial check
  animateOnScroll();
</script>