---
import BaseLayout from '../layouts/BaseLayout.astro';

// Enhanced type definitions for warehouse data
interface WarehouseData {
  id: string;
  title: string;
  slug: string;
  price: string;
  size: number;
  location: string;
  address: string;
  features: string[];
  images: string[];
  description: string;
  availability: 'available' | 'leased' | 'pending';
  featured: boolean;
  createdAt: string;
}

interface WarehouseCollection {
  data: WarehouseData;
  id: string;
  collection: string;
}

// Load warehouses with proper typing
const displayWarehouses: WarehouseCollection[] = [
  {
    id: 'warehouse-1',
    collection: 'warehouses',
    data: {
      id: 'warehouse-1',
      title: 'Modern Distribution Center',
      slug: 'modern-distribution-center',
      price: '$12/sq ft/year',
      size: 50000,
      location: 'Miami, FL',
      address: '123 Industrial Blvd, Miami, FL 33178',
      features: ['Dock High', '24/7 Access', 'Climate Controlled'],
      images: ['/images/warehouse-1.jpg', '/images/warehouse-1-2.jpg'],
      description: 'Premium warehouse space with modern amenities',
      availability: 'available' as const,
      featured: true,
      createdAt: '2024-01-15'
    }
  },
  {
    id: 'warehouse-2', 
    collection: 'warehouses',
    data: {
      id: 'warehouse-2',
      title: 'Flex Space Industrial',
      slug: 'flex-space-industrial',
      price: '$15/sq ft/year',
      size: 25000,
      location: 'Fort Lauderdale, FL',
      address: '456 Commerce Way, Fort Lauderdale, FL 33309',
      features: ['Office Space', 'Loading Docks', 'Parking'],
      images: ['/images/warehouse-2.jpg'],
      description: 'Flexible industrial space perfect for growing businesses',
      availability: 'available' as const,
      featured: true,
      createdAt: '2024-01-20'
    }
  },
  {
    id: 'warehouse-3',
    collection: 'warehouses', 
    data: {
      id: 'warehouse-3',
      title: 'Logistics Hub Premium',
      slug: 'logistics-hub-premium',
      price: '$18/sq ft/year',
      size: 75000,
      location: 'Doral, FL',
      address: '789 Logistics Dr, Doral, FL 33172',
      features: ['Rail Access', 'Cross Dock', 'Security'],
      images: ['/images/warehouse-3.jpg'],
      description: 'State-of-the-art logistics facility with premium features',
      availability: 'pending' as const,
      featured: true,
      createdAt: '2024-01-25'
    }
  }
];

// Filter to only featured warehouses for homepage
const featuredWarehouses = displayWarehouses.filter(warehouse => warehouse.data.featured);
---

<BaseLayout 
  title="Premium Warehouse Leasing | Ariel Realty Group"
  description="Find your perfect warehouse space in South Florida. Expert tenant representation, premium properties, and unmatched service."
>
  <!-- Hero Section -->
  <section class="relative min-h-screen flex items-center justify-center overflow-hidden bg-gradient-to-br from-slate-900 via-slate-800 to-slate-900">
    <!-- Background Pattern -->
    <div class="absolute inset-0 bg-pattern-grid opacity-20"></div>
    
    <!-- Floating Elements -->
    <div class="absolute top-20 left-10 w-20 h-20 bg-blue-500/20 rounded-full blur-xl float"></div>
    <div class="absolute bottom-32 right-16 w-32 h-32 bg-cyan-500/20 rounded-full blur-xl float-reverse"></div>
    <div class="absolute top-1/2 left-1/4 w-16 h-16 bg-purple-500/20 rounded-full blur-xl float"></div>
    
    <div class="relative z-10 max-w-7xl mx-auto px-6 lg:px-8 text-center">
      <!-- Hero Content -->
      <div class="gsap-fade-in">
        <h1 class="text-5xl lg:text-7xl font-black text-white leading-tight tracking-tight mb-8">
          PREMIUM
          <span class="block text-gradient">WAREHOUSE</span>
          <span class="block text-white">LEASING</span>
        </h1>
      </div>
      
      <div class="gsap-fade-in">
        <p class="text-xl lg:text-2xl text-slate-300 max-w-4xl mx-auto mb-12 leading-relaxed">
          Expert tenant representation for South Florida's most sought-after industrial properties. 
          <span class="text-gradient font-semibold">Zero fees. Maximum value.</span>
        </p>
      </div>

      <!-- CTA Buttons -->
      <div class="gsap-fade-in flex flex-col sm:flex-row gap-6 justify-center items-center">
        <a href="/warehouses" class="group bg-gradient-to-r from-blue-600 to-cyan-600 text-white px-8 py-4 rounded-2xl font-bold text-lg hover:shadow-2xl hover:-translate-y-1 transition-all duration-300 btn-premium">
          <span class="flex items-center gap-3">
            Explore Properties
            <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
            </svg>
          </span>
        </a>
        
        <a href="/contact" class="group bg-white/10 backdrop-blur-xl text-white border border-white/20 px-8 py-4 rounded-2xl font-bold text-lg hover:bg-white/20 hover:-translate-y-1 transition-all duration-300">
          <span class="flex items-center gap-3">
            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"></path>
            </svg>
            Get Expert Help
          </span>
        </a>
      </div>

      <!-- Trust Indicators -->
      <div class="gsap-fade-in mt-16 grid grid-cols-1 md:grid-cols-3 gap-8 max-w-4xl mx-auto">
        <div class="text-center">
          <div class="text-3xl lg:text-4xl font-black text-gradient mb-2 counter" data-target="100">0</div>
          <div class="text-slate-400 font-medium">Properties Managed</div>
        </div>
        <div class="text-center">
          <div class="text-3xl lg:text-4xl font-black text-gradient mb-2 counter" data-target="500">0</div>
          <div class="text-slate-400 font-medium">Successful Leases</div>
        </div>
        <div class="text-center">
          <div class="text-3xl lg:text-4xl font-black text-gradient mb-2">$0</div>
          <div class="text-slate-400 font-medium">Tenant Fees</div>
        </div>
      </div>
    </div>

    <!-- Scroll Indicator -->
    <div class="absolute bottom-8 left-1/2 transform -translate-x-1/2 gsap-fade-in">
      <div class="animate-bounce">
        <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
        </svg>
      </div>
    </div>
  </section>

  <!-- Featured Properties Section -->
  <section class="relative py-32 bg-slate-50 overflow-hidden">
    <!-- Section Number -->
    <div class="absolute top-12 left-12 text-slate-300 font-mono text-lg tracking-wider gsap-fade-in-left">
      01 // 04
    </div>

    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <!-- Section Header -->
      <div class="text-center mb-20">
        <h2 class="text-5xl lg:text-6xl font-black text-slate-900 leading-tight tracking-tight mb-8 gsap-fade-in">
          FEATURED
          <span class="block text-gradient">PROPERTIES</span>
        </h2>
        <p class="text-xl text-slate-600 max-w-2xl mx-auto leading-relaxed gsap-fade-in">
          Premium warehouse spaces across South Florida, hand-picked for their quality and value
        </p>
      </div>

      <!-- Properties Grid -->
      <div class="grid lg:grid-cols-3 gap-8 mb-16">
        {featuredWarehouses.map((warehouse, index) => (
          <div class="gsap-stagger group">
            <div class="bg-white rounded-3xl overflow-hidden shadow-premium hover:shadow-premium-lg transition-all duration-500 transform hover:-translate-y-2">
              <!-- Image -->
              <div class="relative aspect-w-16 aspect-h-10 overflow-hidden">
                <img 
                  src={warehouse.data.images?.[0] || '/images/placeholder-warehouse.jpg'}
                  alt={warehouse.data.title}
                  class="w-full h-64 object-cover group-hover:scale-105 transition-transform duration-500"
                  loading="lazy"
                />
                <div class="absolute top-4 right-4">
                  <span class={`px-3 py-1 rounded-full text-xs font-bold uppercase tracking-wide ${
                    warehouse.data.availability === 'available' 
                      ? 'bg-green-100 text-green-800' 
                      : warehouse.data.availability === 'pending'
                      ? 'bg-yellow-100 text-yellow-800'
                      : 'bg-red-100 text-red-800'
                  }`}>
                    {warehouse.data.availability === 'available' ? '✅ Available' : 
                     warehouse.data.availability === 'pending' ? '⏳ Pending' : '🔒 Leased'}
                  </span>
                </div>
              </div>

              <!-- Content -->
              <div class="p-8">
                <div class="flex items-start justify-between mb-4">
                  <h3 class="text-2xl font-bold text-slate-900 group-hover:text-blue-600 transition-colors duration-300">
                    {warehouse.data.title}
                  </h3>
                  <div class="text-right">
                    <div class="text-2xl font-black text-gradient">
                      {warehouse.data.price}
                    </div>
                  </div>
                </div>

                <div class="space-y-3 mb-6">
                  <div class="flex items-center text-slate-600">
                    <svg class="w-5 h-5 mr-3 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                    </svg>
                    <span class="font-medium">{warehouse.data.location}</span>
                  </div>
                  <div class="flex items-center text-slate-600">
                    <svg class="w-5 h-5 mr-3 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 8V6a2 2 0 012-2h2M4 8v8a2 2 0 002 2h2m0-16v16m8-16v16m0-16h2a2 2 0 012 2v2m-4 8h4"/>
                    </svg>
                    <span class="font-medium">{warehouse.data.size.toLocaleString()} sq ft</span>
                  </div>
                </div>

                <!-- Features -->
                <div class="flex flex-wrap gap-2 mb-6">
                  {warehouse.data.features.slice(0, 3).map((feature) => (
                    <span class="px-3 py-1 bg-blue-50 text-blue-700 rounded-lg text-sm font-medium">
                      {feature}
                    </span>
                  ))}
                </div>

                <!-- CTA -->
                <a 
                  href={`/warehouses/${warehouse.data.slug}`}
                  class="block w-full bg-gradient-to-r from-blue-600 to-cyan-600 text-white text-center py-3 rounded-xl font-semibold hover:shadow-lg hover:-translate-y-0.5 transition-all duration-300 btn-premium"
                >
                  View Details
                </a>
              </div>
            </div>
          </div>
        ))}
      </div>

      <!-- View All CTA -->
      <div class="text-center gsap-fade-in">
        <a href="/warehouses" class="group inline-flex items-center gap-3 bg-white text-slate-900 px-8 py-4 rounded-2xl font-bold text-lg shadow-premium hover:shadow-premium-lg hover:-translate-y-1 transition-all duration-300">
          View All Properties
          <svg class="w-5 h-5 group-hover:translate-x-1 transition-transform" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Why Choose Us Section -->
  <section class="relative py-32 bg-white overflow-hidden">
    <!-- Section Number -->
    <div class="absolute top-12 right-12 text-slate-300 font-mono text-lg tracking-wider gsap-fade-in-right">
      02 // 04
    </div>

    <div class="max-w-7xl mx-auto px-6 lg:px-8">
      <div class="grid lg:grid-cols-2 gap-16 items-center">
        <!-- Content -->
        <div class="gsap-fade-in-left">
          <h2 class="text-5xl lg:text-6xl font-black text-slate-900 leading-tight tracking-tight mb-8">
            WHY CHOOSE
            <span class="block text-gradient">OUR EXPERTISE</span>
          </h2>
          <p class="text-xl text-slate-600 leading-relaxed mb-8">
            With decades of experience in South Florida's industrial market, we provide unmatched tenant representation that puts your interests first.
          </p>
          
          <div class="space-y-6">
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-blue-100 rounded-xl flex items-center justify-center flex-shrink-0">
                <svg class="w-6 h-6 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1"/>
                </svg>
              </div>
              <div>
                <h3 class="text-xl font-bold text-slate-900 mb-2">Zero Tenant Fees</h3>
                <p class="text-slate-600">Our services are completely free to tenants. Landlords pay our commission.</p>
              </div>
            </div>
            
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-green-100 rounded-xl flex items-center justify-center flex-shrink-0">
                <svg class="w-6 h-6 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
                </svg>
              </div>
              <div>
                <h3 class="text-xl font-bold text-slate-900 mb-2">Expert Negotiation</h3>
                <p class="text-slate-600">We secure the best terms, pricing, and conditions for your lease.</p>
              </div>
            </div>
            
            <div class="flex items-start gap-4">
              <div class="w-12 h-12 bg-purple-100 rounded-xl flex items-center justify-center flex-shrink-0">
                <svg class="w-6 h-6 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"/>
                </svg>
              </div>
              <div>
                <h3 class="text-xl font-bold text-slate-900 mb-2">Speed & Efficiency</h3>
                <p class="text-slate-600">Find and secure your ideal space faster with our proven process.</p>
              </div>
            </div>
          </div>
        </div>

        <!-- Visual -->
        <div class="gsap-fade-in-right">
          <div class="relative">
            <div class="bg-gradient-to-br from-blue-600 to-cyan-600 rounded-3xl p-8 text-white shadow-premium">
              <h3 class="text-2xl font-bold mb-6">Market Advantage</h3>
              <div class="space-y-6">
                <div class="flex items-center justify-between">
                  <span>Properties Managed</span>
                  <span class="font-bold counter" data-target="100">0</span>
                </div>
                <div class="flex items-center justify-between">
                  <span>Average Savings</span>
                  <span class="font-bold">25%</span>
                </div>
                <div class="flex items-center justify-between">
                  <span>Client Satisfaction</span>
                  <span class="font-bold">98%</span>
                </div>
                <div class="flex items-center justify-between">
                  <span>Market Coverage</span>
                  <span class="font-bold">South FL</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="relative py-32 bg-gradient-to-br from-slate-900 to-slate-800 overflow-hidden">
    <!-- Background Elements -->
    <div class="absolute inset-0 bg-pattern-dots opacity-10"></div>
    
    <!-- Section Number -->
    <div class="absolute top-12 left-12 text-slate-500 font-mono text-lg tracking-wider gsap-fade-in-left">
      03 // 04
    </div>

    <div class="relative z-10 max-w-4xl mx-auto px-6 lg:px-8 text-center">
      <div class="gsap-scale-in">
        <h2 class="text-5xl lg:text-6xl font-black text-white leading-tight tracking-tight mb-8">
          READY TO FIND YOUR
          <span class="block text-gradient">PERFECT SPACE?</span>
        </h2>
        <p class="text-xl text-slate-300 mb-12 leading-relaxed">
          Let our experts guide you to the ideal warehouse solution. Zero fees, maximum value, proven results.
        </p>
        
        <div class="flex flex-col sm:flex-row gap-6 justify-center items-center">
          <a href="/contact" class="bg-gradient-to-r from-blue-600 to-cyan-600 text-white px-8 py-4 rounded-2xl font-bold text-lg hover:shadow-2xl hover:-translate-y-1 transition-all duration-300 btn-premium">
            Start Your Search
          </a>
          <a href="tel:+15551234567" class="bg-white/10 backdrop-blur-xl text-white border border-white/20 px-8 py-4 rounded-2xl font-bold text-lg hover:bg-white/20 hover:-translate-y-1 transition-all duration-300">
            Call Now: (555) 123-4567
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<script>
  // Enhanced page-specific animations
  function initPageAnimations(): void {
    // Ensure GSAP is available
    if (typeof gsap === 'undefined') {
      setTimeout(initPageAnimations, 100);
      return;
    }

    // Property card hover animations
    const propertyCards = document.querySelectorAll('.gsap-stagger');
    propertyCards.forEach((card: Element) => {
      const cardElement = card as HTMLElement;
      
      cardElement.addEventListener('mouseenter', () => {
        gsap.to(cardElement, { 
          scale: 1.02, 
          duration: 0.3, 
          ease: "power2.out" 
        });
      });
      
      cardElement.addEventListener('mouseleave', () => {
        gsap.to(cardElement, { 
          scale: 1, 
          duration: 0.3, 
          ease: "power2.out" 
        });
      });
    });

    // Enhanced scroll indicators
    gsap.to('.animate-bounce', {
      y: 10,
      duration: 1,
      ease: "power2.inOut",
      yoyo: true,
      repeat: -1
    });

    console.log('✅ Page-specific animations initialized');
  }

  // Initialize when DOM is ready
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', initPageAnimations);
  } else {
    initPageAnimations();
  }
</script>